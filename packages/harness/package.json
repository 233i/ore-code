{
  "name": "@seekforge/harness",
  "private": true,
  "version": "0.1.0",
  "description": "SeekForge deterministic scenario replay and harness tests",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @seekforge/protocol build && pnpm --filter @seekforge/tools build && pnpm --filter @seekforge/agent-core build && vitest run src",
    "lint": "eslint src --ext .ts"
  },
  "dependencies": {
    "@seekforge/agent-core": "workspace:*",
    "@seekforge/protocol": "workspace:*",
    "@seekforge/state": "workspace:*",
    "@seekforge/tools": "workspace:*"
  },
  "devDependencies": {
    "typescript": "~5.8.3",
    "vitest": "^4.0.8"
  }
}
